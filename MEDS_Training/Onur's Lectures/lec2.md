# Digital Design and Computer Architecture

## L2: Combinational Logic

### Gernal CMOS:

if the both (n-type and p-type structure) ON at same time then there is short circuit
if the both (n-type and p-type structure) OFF at same time then it will undefined output
Latancy: series transistors are slower due to capacitence of wire than paralle conected transistors

### Moore's Law:

Number of transistors doubled on microchip after every 2 years

### Tpes of Logic Circuits:

Combinational: memoryless , ouput only depends on the input
Sequential: use memory , output depends on the input+previous state/results

### Duality:

AND and OR gate duality by taking complement AND gate will Change Into OR and vice versa

### SOP

sum of products , minterns represented as 1 -> A=1, A' = 0

### POS

product of Sum , maxterms represented as 0 -> A=0, A' = 1

### Combinational circuits

1. Decoder
2. multiplex's/Selector
3. Programmable Logic Array (FPGA)
